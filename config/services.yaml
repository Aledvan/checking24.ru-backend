# This file is the entry point to configure your own services.
# Files in the packages/ subdirectory configure your dependencies.

# Put parameters here that don't need to change on each machine where the app is deployed
# https://symfony.com/doc/current/best_practices.html#use-parameters-for-application-configuration
parameters:
    app.locale: '%env(APP_LOCALE)%'
    app.jwt_secret: '%env(JWT_SECRET)%'
    app.jwt_ttl: '%env(int:JWT_TTL)%'
    app.refresh_token_ttl: '%env(int:REFRESH_TOKEN_TTL)%'
    app.mailer_from: '%env(MAILER_FROM)%'
    app.frontend_url: '%env(FRONTEND_URL)%'

services:
    # default configuration for services in *this* file
    _defaults:
        autowire: true      # Automatically injects dependencies in your services.
        autoconfigure: true # Automatically registers your services as commands, event subscribers, etc.

    # makes classes in src/ available to be used as services
    # this creates a service per class whose id is the fully-qualified class name
    App\:
        resource: '../src/'
        exclude:
            - '../src/DependencyInjection/'
            - '../src/Entity/'
            - '../src/Kernel.php'

    # add more service definitions when explicit configuration is needed
    # please note that last definitions always *replace* previous ones

    App\Service\Translator:
        arguments:
            $locale: '%app.locale%'

    App\Service\JwtService:
        arguments:
            $secret: '%app.jwt_secret%'
            $ttl: '%app.jwt_ttl%'

    App\Service\TokenService:
        arguments:
            $refreshTokenTtl: '%app.refresh_token_ttl%'

    App\Service\EmailService:
        arguments:
            $fromEmail: '%app.mailer_from%'
            $frontendUrl: '%app.frontend_url%'

    App\EventListener\ExceptionListener:
        tags:
            - { name: kernel.event_listener, event: kernel.exception }

    # Monitoring services use dedicated logging channel
    App\Service\SiteService:
        arguments:
            $logger: '@monolog.logger.monitoring'

    App\Service\SiteCheckerService:
        arguments:
            $logger: '@monolog.logger.monitoring'

    App\MessageHandler\CheckSiteMessageHandler:
        arguments:
            $logger: '@monolog.logger.monitoring'

    App\MessageHandler\DispatchSiteChecksMessageHandler:
        arguments:
            $logger: '@monolog.logger.monitoring'
